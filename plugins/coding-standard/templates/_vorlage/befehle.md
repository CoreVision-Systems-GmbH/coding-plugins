| Zweck                        | Befehl                    |
| ---------------------------- | ------------------------- |
| Alles prüfen                 | `<befehl>`                |
| Tests                        | `<befehl>`                |
| Formatierung prüfen / setzen | `<befehl>` / `<befehl>`   |
| Bauen                        | `<befehl>`                |
| Entwicklung                  | `<befehl>`                |

<!-- Dieser Block landet in der CLAUDE.md des neuen Projekts anstelle von {{COMMANDS}}.
     Nur Befehle eintragen, die es nach dem Gerüst wirklich gibt — Claude übernimmt sie
     von hier und erfindet keine. -->
