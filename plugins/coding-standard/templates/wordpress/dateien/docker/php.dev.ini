; Nur im lokalen Verbund (compose.dev.yaml): Theme und Mu-Plugins sind aus dem
; Arbeitsordner eingebunden — Änderungen sollen ohne Neustart sichtbar sein.
opcache.validate_timestamps = 1
opcache.revalidate_freq = 0
