# Stack-Register: FastAPI
LABEL="FastAPI · Python 3.12"
DESCRIPTION="HTTP-Dienst oder internes Werkzeug mit JSON-Schnittstelle, Docker-Lieferweg"
REQUIRES="python"
CONTAINERIZED=1
MARKER=0
# Zieldatenbank, landet in der CLAUDE.md des Projekts.
DATABASE="SQLite (Datei unter DATA_DIR)"
# Produkte mit mehreren Nutzern oder Wachstum: PostgreSQL 17 — dann diesen Wert in der
# CLAUDE.md des Projekts ändern, bevor die erste Migration entsteht.
